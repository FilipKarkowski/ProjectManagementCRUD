<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @project.name %></title>
  <!-- Tailwind CSS -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">

<div class="container mx-auto py-8">
  <% if notice %>
    <p class="text-green-500 mb-4"><%= notice %></p>
  <% end %>

  <div class="bg-white shadow-md rounded-md p-4">
    <%= render @project %>

    <div class="mt-4 flex justify-between">
      <%= link_to "Powrót do projektów", projects_path, class: "text-blue-500 hover:text-blue-700" %>
      <%= link_to "Edytuj ten projekt", edit_project_path(@project), class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
      <%= button_to "Usuń ten projekt", @project, method: :delete, data: { confirm: "Czy na pewno chcesz usunąć ten projekt?" }, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
  </div>

  <h3 class="text-xl font-semibold mt-8 mb-4">Zadania:</h3>
  <% if @project.tasks.any? %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      <% @project.tasks.each do |task| %>
        <div class="bg-gray-200 p-4 rounded-md">
          <h4 class="font-semibold"><%= task.name %></h4>
          <p><%= task.description %></p>
        </div>
      <% end %>
    </div>
  <% else %>
    <p>Brak zadań dla tego projektu.</p>
  <% end %>
</div>

</body>
</html>
