<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Projekty</title>
  <!-- Tailwind CSS -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <!-- Turbo Frames -->
  <script src="https://cdn.jsdelivr.net/npm/@hotwired/turbo@7.0.0/build/turbo.js" defer></script>
</head>
<body class="bg-gray-100">

<div class="container mx-auto py-8">
  <h1 class="text-3xl font-semibold mb-4">Projekty</h1>

  <% if notice %>
    <p class="text-green-500 mb-4"><%= notice %></p>
  <% end %>

  <div id="projects" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    <% @projects.each do |project| %>
      <div class="bg-white shadow-md rounded-md p-4">
        <%= render project %>
        <div class="mt-4 flex justify-between">
          <p><%= link_to "Pokaż ten projekt", project, class: "text-blue-500 hover:text-blue-700" %></p>
          <!-- Dodano przycisk do usuwania projektu -->
          <%= link_to "Usuń", project, method: :delete, data: { confirm: "Czy na pewno chcesz usunąć?" }, class: "text-red-500 hover:text-red-700" %>
        </div>
      </div>
    <% end %>
  </div>

  <div class="mt-8">
    <!-- Użyj Turbo Frame do ładowania formularza nowego projektu -->
    <turbo-frame id="new-project-form" src="<%= new_project_path %>"></turbo-frame>
    <%= link_to "Nowy projekt", "/projects/new", class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded", data: { turbo_frame: "new-project-form" } %>
  </div>
</div>

</body>
</html>
